#include <iostream>
#include <cstdlib>
#include <vector>

using namespace std;

int main(int argc, char* argv[])
{
    

    // Vector demonstration
    // int arrSize = 5;
    // int* numbers = new int[arrSize];
    // vector<int> vectorNumbers;
    // // int* numbers;
    // // numbers = (int*)calloc(arrSize, sizeof(int));
    // // int* newNumbers = (int*)calloc(arrSize+10, sizeof(int));
    // cout << vectorNumbers.capacity() << endl;
    // for(int i = 0; i < arrSize; i++)
    // {
    //     numbers[i] = i*10;
    //     vectorNumbers.push_back(i*10);
    //     // cout << i << endl;
    // }

    // for(int i = 0; i < arrSize; i++)
    // {
    //     cout << numbers[i] << " ";
    // }
    // cout << endl;
    // for(int i = 0; i < arrSize; i++)
    // {
    //     cout << vectorNumbers.at(i) << " ";
    // }
    // cout << endl;
    // cout << vectorNumbers.capacity() << endl;

    return 0;
}